import styled from "styled-components";

export const Input = styled.input`
  padding: 10px 20px;
  width: 25%;
  font-size: 16px;
  border-radius: 8px;
  border: none;
  border: 1px solid rgba(0, 0, 0, 0.2);
`